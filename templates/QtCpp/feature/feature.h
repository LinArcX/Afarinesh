#ifndef {*TOKEN*}_H
#define {*TOKEN*}_H

#include <QObject>

class {*TOKEN*} : public QObject {
    Q_OBJECT

public:
    explicit {*TOKEN*}(QObject* parent = nullptr);

public slots:

signals:

private:
};

#endif // {*TOKEN*}_H
